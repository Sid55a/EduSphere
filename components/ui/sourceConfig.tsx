import { Profile } from "@prisma/client";
import { Layer, Source } from "react-map-gl";
interface SourceConfigProps {
  other: any;
  me: any;
}
export const SourceConfig = ({ other, me }: SourceConfigProps) => {
 
  console.log("heeee");
  return (
    <>
     
    </>
  );
};
